<?xml version="1.0" encoding="utf-8"?>
<odoo>
 -->

    <record id="action_dashboard" model="ir.actions.client">
        <field name="name">Dashboard</field>
        <field name="tag">am_dashboard</field>
        <field name="params" eval="{}"/>
    </record>

    <!-- QUICK ACTION DIALOG: Register Asset -->

    <record id="action_quick_register_asset" model="ir.actions.act_window">
        <field name="name">Register Asset</field>
        <field name="res_model">asset.asset</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- QUICK ACTION DIALOG: Book Resource -->

    <record id="action_quick_book_resource" model="ir.actions.act_window">
        <field name="name">Book Resource</field>
        <field name="res_model">resource.booking</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- QUICK ACTION DIALOG: Raise Maintenance -->

    <record id="action_quick_maintenance" model="ir.actions.act_window">
        <field name="name">Raise Maintenance Request</field>
        <field name="res_model">am.maintenance.request</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- QUICK ACTION DIALOG: Request Transfer -->

    <record id="action_quick_transfer" model="ir.actions.act_window">
        <field name="name">Request Transfer</field>
        <field name="res_model">asset.transfer</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
